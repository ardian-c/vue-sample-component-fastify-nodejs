export const payments = state => state.payments;

export const query = state => state.query;

export const isLoading = state => state.isLoading;

export const successMessage = state => state.successMessage;

export const failedMessage = state => state.failedMessage;
